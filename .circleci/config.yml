version: 2
jobs:
  run-test:
    docker:
      - image: circleci/node:8.10
    steps:
      - checkout
      - run: npm install
      - run: npm run test
workflows:
  version: 2
  run-jobs:
    jobs:
      - run-test
